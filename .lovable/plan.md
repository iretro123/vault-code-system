

## Rename Community Tab Labels

**File:** `src/pages/academy/AcademyCommunity.tsx` — lines 8-12

Change the `TABS` array labels:
- `"Trade Floor"` → `"Chat"`
- `"Daily Setups"` → `"Signals"`
- `"Wins & Proof"` → `"Wins"`

Three string changes, no logic changes.

