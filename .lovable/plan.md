

## Change Button Text in NextGroupCallCard

**File: `src/components/academy/dashboard/NextGroupCallCard.tsx`** — ~line 72

Change the "View Replays" text to "View Calls" in the empty-state button (when no upcoming calls are scheduled).

