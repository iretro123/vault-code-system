

## Rename "Your Gameplan" → "Your Onboarding"

Single text change in `src/components/academy/dashboard/GameplanCard.tsx`.

**Line ~107**: Change the heading from `"Your Gameplan"` to `"Your Onboarding"`.

That's it — one line, no logic changes.

