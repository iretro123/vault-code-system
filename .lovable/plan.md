

## Remove Trade Format Template from Guidelines Modal

**File:** `src/components/academy/community/GuidelinesModal.tsx`

### Changes

1. **Remove the "Trade Format Template" section** (lines 79-96): Delete the entire block containing the `<p>` label, the `<pre>` code block, and the copy button
2. **Keep everything else**: Rules of the Room, Disclaimer, and Moderation note all stay intact

This is a simple content removal — no layout or responsive changes needed since removing a section naturally flows on all devices.

